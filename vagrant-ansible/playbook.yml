---
- name: Install zimg
  hosts: default
  vars_files:
    - ./env.yaml
  roles:
    - zimg

- name: Install ffmpeg
  hosts: default
  vars_files:
    - ./env.yaml
  roles:
    - ffmpeg

- name: Install vapoursynth
  hosts: default
  vars_files:
    - ./env.yaml
  roles:
    - vapoursynth

- name: Install vsedit
  hosts: default
  vars_files:
    - ./env.yaml
  roles:
    - vsedit

# d2vwitch, d2vsource, plugins (damb, fmtconv, ffms2, mvtools, fft3dfilter, nnedi3, znedi3, dfttest, bm3d), vsscripts, test-files
